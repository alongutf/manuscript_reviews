# revision plan for figure 5
figure 5 introduces the vapC experiment as another example for dysregulation.

**main points that need to be revised**
- add the schematic image `scripts\figures\figure5\biorender2.svg` to panel A but must maintain vector format.
- change panels D, E to the new ccdf format (as in the updated figure3). The eigenvalue data for the relevant samples is stored in `ev_data`
- Panel F needs to be adjusted to the new GMP-Cor metric, the results are in `results\data_metrics\test8.csv` use the column 'sum_denoised_ev'.